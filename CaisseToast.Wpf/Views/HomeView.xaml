<UserControl x:Class="CaisseToast.Wpf.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource Brush.Background}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Mode sub-header -->
        <Border Grid.Row="0" Background="{StaticResource Brush.HeaderGradient}" Height="56" Padding="32,0">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="36" Height="36" CornerRadius="10" Background="#1AFFFFFF" Margin="0,0,12,0">
                        <TextBlock Text="H" Foreground="White" FontWeight="Bold" FontSize="16"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Foreground="White" FontSize="15" FontWeight="Bold">
                            <Run Text="{Binding UserName, Mode=OneWay}"/>
                            <Run Text=" — Accueil caisse"/>
                        </TextBlock>
                        <TextBlock Text="Point de vente · Caisse 1 · Service en cours"
                                   Foreground="#B3D1EFFE" FontSize="10" FontWeight="SemiBold"/>
                    </StackPanel>
                </StackPanel>
                <Button Content="Switch user" Style="{StaticResource Btn.HeaderGhost}"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Command="{Binding SwitchUserCommand}"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="32,24">
            <StackPanel MaxWidth="1280">
                <!-- KPI strip -->
                <UniformGrid Columns="4" Margin="0,0,0,20">
                    <Border Style="{StaticResource KpiCard}" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="{Binding OpenOrders}" FontSize="26" FontWeight="Bold" Foreground="{StaticResource Brush.Navy}"/>
                            <TextBlock Text="COMMANDES OUVERTES" FontSize="9" FontWeight="Bold" Foreground="{StaticResource Brush.Muted}" Margin="0,6,0,0"/>
                            <TextBlock Text="En attente" FontSize="11" Foreground="{StaticResource Brush.Blue}" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource KpiCard}" Margin="4,0">
                        <StackPanel>
                            <TextBlock Text="{Binding PaidToday}" FontSize="26" FontWeight="Bold" Foreground="{StaticResource Brush.Navy}"/>
                            <TextBlock Text="ENCAISSÉES AUJOURD'HUI" FontSize="9" FontWeight="Bold" Foreground="{StaticResource Brush.Muted}" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource KpiCard}" Margin="4,0">
                        <StackPanel>
                            <TextBlock Text="{Binding Revenue}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource Brush.Navy}"/>
                            <TextBlock Text="CHIFFRE D'AFFAIRES" FontSize="9" FontWeight="Bold" Foreground="{StaticResource Brush.Muted}" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource KpiCard}" Margin="8,0,0,0">
                        <StackPanel>
                            <TextBlock Text="{Binding TablesInfo}" FontSize="26" FontWeight="Bold" Foreground="{StaticResource Brush.Navy}"/>
                            <TextBlock Text="TABLES" FontSize="9" FontWeight="Bold" Foreground="{StaticResource Brush.Muted}" Margin="0,6,0,0"/>
                            <TextBlock Text="{Binding TablesSub}" FontSize="11" Foreground="{StaticResource Brush.Blue}" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- Hero tiles -->
                <UniformGrid Columns="3" Margin="0,0,0,24">
                    <Button Style="{StaticResource HeroTile}" Margin="0,0,8,0" Command="{Binding OpenPosCommand}">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="52" Height="52" CornerRadius="13" Background="{StaticResource Brush.PrimaryButton}" Margin="0,0,14,0">
                                <TextBlock Text="Q" Foreground="White" FontSize="22" FontWeight="Bold"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Quick Order" FontSize="16" FontWeight="Bold" Foreground="{StaticResource Brush.Navy}"/>
                                <TextBlock Text="Prise de commande rapide sans table" FontSize="11" Foreground="{StaticResource Brush.Muted}" Margin="0,4,0,0" TextWrapping="Wrap" MaxWidth="220"/>
                            </StackPanel>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource HeroTile}" Margin="4,0" Command="{Binding OpenTerminalCommand}">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="52" Height="52" CornerRadius="13" Background="{StaticResource Brush.PrimaryButton}" Margin="0,0,14,0">
                                <TextBlock Text="T" Foreground="White" FontSize="22" FontWeight="Bold"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Payment Terminal" FontSize="16" FontWeight="Bold" Foreground="{StaticResource Brush.Navy}"/>
                                <TextBlock Text="Open · Paid · Closed" FontSize="11" Foreground="{StaticResource Brush.Muted}" Margin="0,4,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource HeroTile}" Margin="8,0,0,0" Command="{Binding OpenTablesCommand}">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="52" Height="52" CornerRadius="13" Background="{StaticResource Brush.PrimaryButton}" Margin="0,0,14,0">
                                <TextBlock Text="S" Foreground="White" FontSize="22" FontWeight="Bold"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Table Service" FontSize="16" FontWeight="Bold" Foreground="{StaticResource Brush.Navy}"/>
                                <TextBlock Text="Plan de salle interactif" FontSize="11" Foreground="{StaticResource Brush.Muted}" Margin="0,4,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Button>
                </UniformGrid>

                <!-- Service & Canaux panel -->
                <Border Background="White" BorderBrush="#140B128C" BorderThickness="1" CornerRadius="18" Padding="18" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Service &amp; Canaux" FontSize="16" FontWeight="Bold" Foreground="{StaticResource Brush.Navy}"/>
                        <TextBlock Text="Commandes, cuisine, bornes et canaux digitaux" FontSize="12" Foreground="{StaticResource Brush.Muted}" Margin="0,4,0,14"/>
                        <UniformGrid Columns="4" Rows="2">
                            <Button Style="{StaticResource HeroTile}" Margin="4" Command="{Binding OpenHubCommand}">
                                <StackPanel>
                                    <Border Width="44" Height="44" CornerRadius="12" Background="{StaticResource Brush.PrimaryButton}" HorizontalAlignment="Center" Margin="0,0,0,8">
                                        <TextBlock Text="H" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="Orders Hub" FontWeight="Bold" Foreground="{StaticResource Brush.Navy}" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource HeroTile}" Margin="4" Command="{Binding OpenKitchenCommand}">
                                <StackPanel>
                                    <Border Width="44" Height="44" CornerRadius="12" Background="{StaticResource Brush.PrimaryButton}" HorizontalAlignment="Center" Margin="0,0,0,8">
                                        <TextBlock Text="K" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="Kitchen Display" FontWeight="Bold" Foreground="{StaticResource Brush.Navy}" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource HeroTile}" Margin="4" Command="{Binding OpenKioskCommand}">
                                <StackPanel>
                                    <Border Width="44" Height="44" CornerRadius="12" Background="{StaticResource Brush.PrimaryButton}" HorizontalAlignment="Center" Margin="0,0,0,8">
                                        <TextBlock Text="B" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="Kiosk" FontWeight="Bold" Foreground="{StaticResource Brush.Navy}" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource HeroTile}" Margin="4" IsEnabled="False" Opacity="0.5">
                                <StackPanel>
                                    <Border Width="44" Height="44" CornerRadius="12" Background="#CCC" HorizontalAlignment="Center" Margin="0,0,0,8">
                                        <TextBlock Text="O" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="Online Ordering" FontWeight="Bold" Foreground="{StaticResource Brush.Muted}" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </UniformGrid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
