<UserControl x:Class="CaisseToast.Wpf.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource Brush.Background}">
    <Grid>
        <Border Background="White" BorderBrush="#140B128C" BorderThickness="1"
                CornerRadius="18" Padding="48" MaxWidth="520"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel HorizontalAlignment="Center">
                <Border Width="64" Height="64" CornerRadius="16" Background="{StaticResource Brush.PrimaryButton}" Margin="0,0,0,20">
                    <TextBlock Text="LB" Foreground="White" FontSize="22" FontWeight="Bold"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="{Binding Title}" FontSize="22" FontWeight="Bold"
                           Foreground="{StaticResource Brush.Navy}" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding Subtitle}" FontSize="13" Foreground="{StaticResource Brush.Muted}"
                           HorizontalAlignment="Center" Margin="0,8,0,24" TextAlignment="Center"/>
                <TextBlock Text="Module en cours de migration depuis la version web."
                           FontSize="12" Foreground="{StaticResource Brush.Muted}" TextAlignment="Center" Margin="0,0,0,20"/>
                <Button Content="Retour à l'accueil" Style="{StaticResource Btn.Primary}"
                        Command="{Binding GoBackCommand}" MinWidth="180"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
